필요한 헤더파일
	stdio.h
	stdlib.h
	unistd.h
	math.h

필요한 구조체
	info : 파일의 맨 첫줄 정보
			wdith, height, big_char
	도형 구조체 : 파일 다음 줄 정보
			rect : type, x, y, w, h, draw_char
			circle : type, x, y, r, draw_char

필요한 함수
	ft_strlen : 문자열 길이 함수
	ft_putstr : 문자열 출력 함수
	is_in_rect/circle : 도형 테두리 함수
	print_map : 맵 그리는 함수
	micro/mini_print : 맵 데이터 넣는 함수

is_in_rect/circle 알고리즘
	is_in_rect/circle
		1. 테두리 좌표를 구해서
		2. 내부면 2, 외부면 0 일치하면 1을 반환

메인 알고리즘
	1. 인자가 두개가 아니면 에러처리
	2. 파일 읽고 파일에 대한 에러처리
		1) 파일에 내용이 존재하는지
		2) 파일의 맨 첫줄 정보가 w, h, bc로 구성되었는지
		3) w, h가 0부터 300까지의 범위 인지
	3. 이차원 배열 map 동적할당 후 bc로 초기화
	4. while 문 돌면서 파일 읽으며 micro/mini_print 실행
		1) 파일 내용이 도형 정보의 개수만큼 나올 때 까지 반복
		2) type이 유효하지 않을 경우 파일 에러처리
	5. 마지막 ret이 -1이 아니면 파일 에러처리
	6. print_map 실행
	7. 이차원 배열 map free